# WhichClaw 后期维护计划

本文档记录待实现的功能和优化方向。

---

## ✅ Skill Browser 动态获取 (已实现)

**当前架构**: 从 GitHub API 动态获取技能列表,本地维护分类映射。

```
GitHub API (实时获取)
    └── 获取技能列表 (目录名)
                ↓
skillsMapping.json (本地分类映射)
    └── 分类、描述覆盖
                ↓
skills.json (离线备份)
    └── GitHub 不可用时回退
```

---

## 🔧 分类映射维护指南

**配置文件**: `electron/config/skillsMapping.json`

### 文件结构

```json
{
  "sources": [
    { "id": "anthropics", "repo": "anthropics/skills", "path": "skills" }
  ],
  "categoryMapping": {
    "frontend-design": "Design",
    "mcp-builder": "Development"
  },
  "defaultCategory": "Other",
  "briefOverrides": {
    "frontend-design": "Create stunning web interfaces"
  }
}
```

### 维护操作

| 操作 | 方法 |
|------|------|
| 添加新技能源 | 在 `sources` 数组添加新仓库 |
| 修改技能分类 | 在 `categoryMapping` 添加/修改映射 |
| 自定义描述 | 在 `briefOverrides` 添加覆盖文字 |
| 新增分类 | 修改 `getSkillCategories()` 函数 |

### 添加技能源示例

```json
{
  "id": "myrepo",           // 唯一标识
  "name": "My Skills",      // 显示名称
  "repo": "username/repo",  // GitHub 仓库路径
  "path": "skills"          // 技能所在子目录,根目录用 ""
}
```

**当前已配置的源:**
- `anthropics/skills` - 官方技能
- `vercel-labs/skills` - Vercel 最佳实践
- `coreyhaines31/marketingskills` - 营销技能
- `ComposioHQ/awesome-claude-skills` - Composio 社区
- `obra/superpowers` - Superpowers 扩展

### 注意事项

- 修改后需执行 `npm run copy:config` 复制到 dist-electron
- 未在 categoryMapping 的技能会使用 `defaultCategory`
- 缓存有效期 5 分钟,强制刷新调用 `refreshSkillsCatalog()`
- 如果仓库结构是根目录放技能,`path` 设为空字符串 `""`

---

## 其他待办

- [ ] **智能提供商协议识别** - `provider-rules.json`
  - 用户输入任意 URL，自动识别提供商
  - 为支持多协议的提供商自动配置两种 URL
  - 例：小米同时支持 `/v1` (OpenAI) 和 `/anthropic/v1` (Anthropic)
- [ ] **账户额度显示** - 显示 Token 使用量、余额、重置时间
- [ ] Model Library 远程模型配置 (参考 skills 实现)
- [ ] Agent Worker 工作流编排
- [ ] 日志导出功能
- [ ] 多语言支持
- [x] ~~添加更多技能源 (ComposioHQ, obra/superpowers 等)~~ ✅ 已完成
